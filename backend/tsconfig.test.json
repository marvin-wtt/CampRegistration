{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,

    // critical: tests are not under ./src
    "rootDir": "."
  },
  "include": [
    "src/**/*.ts",
    "prisma/**/*.ts",
    "tests/**/*.ts",
    "**/*.test.ts",
    "**/*.spec.ts"
  ]
}
